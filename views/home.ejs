<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs') %> 
</head>
<body>
<%- include('./partials/header.ejs') %> 
    <main class="p-4 md:p-8 text-center text-white capitalize mt-5 ">
        <h1 class="text-2xl font-bold text-[orange]" >welcome to my official blog site</h1>
        <p>in this site you will find article about, everything that concern web development</p>
        <p>which include HTML CSS JAVASCRIPT REACT AND NODE.JS</p>
    </main>

<section class="mt-10 p-4 md:p-8 md:grid grid-cols-3 gap-5 break-words md:w-[80%] mx-auto">
    <% blogs.forEach(blog => { %>
        <div class="bg-[#640e64] mb-4 md:mb-0 drop-shadow-lg flex-1">
            <img src="" alt="">
            <div class="p-3 text-white capitalize">
                <h1 class=" text-3xl font-bold">
                    <%= blog.title %>
                        <span class="text-sm bg-pink-300 p-1 rounded">
                            <%=blog.tag%>
                        </span>
                </h1>
                <p class="my-2">
                    <%= blog.doc.slice(0,200) %> ...
                </p>
                <div class="flex gap-2">
                    <a class="btn bg-green-400" href="/blog/<%=blog.id %>">read more</a>
                </div>
            </div>
        </div>
 <%  }); %> 
       
    </section>
</body>
</html>